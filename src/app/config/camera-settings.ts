export const CameraSettings = {
  Alpha: Math.PI / 2, // Horizontal rotation angle around the target
  Beta: Math.PI / 3, // Vertical rotation angle above the target
  Distance: 1200, // Distance from the target
  Target: { x: 0, y: 0, z: 0 }, // Target position
  AttachControl: true, // Enable camera control for user interaction
  LightIntensity: 1, // Default light intensity for scene lighting
};